apiVersion: apps/v1
kind: Deployment
metadata:
  name: pricing
  namespace: verifier-poc
  labels:
    app: pricing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pricing
  template:
    metadata:
      labels:
        app: pricing
    spec:
      containers:
        - name: pricing
          image: docker.io/rinkiyakedad/mirrord-sre-verifier-pricing:dev
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: pricing
  namespace: verifier-poc
spec:
  selector:
    app: pricing
  ports:
    - port: 80
      targetPort: 8080
